Launch Yappa on Linux with ./run-yappa.sh

This is an unsigned development build for limited testing. It is built on the
self-hosted Linux runner so it matches the target distro and desktop stack
more closely than a generic hosted Linux build.
